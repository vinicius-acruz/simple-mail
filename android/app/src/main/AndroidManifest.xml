<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="simple_mail"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <!-- Add these metadata elements for the google sign in and gmail -->
        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.CREDENTIALS_API_KEY"
            android:value="AIzaSyDVoHmx0Pp9jLV8GdIdxxcEgjArEhvD6pc"/>
        <meta-data
            android:name="com.google.android.gms.auth.api.signin.OAUTH_CLIENT_ID"
            android:value="227986853129-3hpqq90kepsgrgpse0s2cd2urcv5086d.apps.googleusercontent.com"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>